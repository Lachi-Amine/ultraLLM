# Assets

Place the following image files here before publishing a build:

- `icon.png` — 1024×1024 app icon
- `splash.png` — splash screen image
- `adaptive-icon.png` — Android adaptive icon foreground

For local development with `npx expo start` these files are not strictly
required — Expo will fall back to defaults. Add them when you ship.
